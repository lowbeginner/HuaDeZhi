# 验证记录

验证日期：2026-09-14。构建环境为 Windows、JDK 21、Android SDK 35；运行环境为 Android 11（API 30）x86_64 模拟器。

## 自动化结果

- `assembleDebug` 与签名的 `assembleRelease` 成功。
- 47 项 JVM/Robolectric 测试全部通过：金额精度、回本计算、输入边界、备份完整往返、损坏/篡改/版本错误拒绝、真实 Room 数据库导出后删除再恢复、数据库重开持久化、写入失败事务回滚、并发次数上限、级联删除、编辑保留历史及跨时区迁移。
- Android 11 模拟器上的 `connectedDebugAndroidTest` 通过：1 项 UI 仪器测试完整执行新建、编辑、删除单次消费，以及新建固定次数项目、记一次使用、验证次数上限并删除。
- Android Lint：0 个错误。依赖版本提示不影响运行，并有意固定到本次验证组合。
- 正式 APK 签名验证通过（APK Signature Scheme v2），包名 `cn.student.expensetracker`，versionCode 1，最低 API 26，目标 API 35。

## 端到端备份迁移

测试账本含晚饭 ¥28、打车 ¥16、咖啡 ¥18、健身年卡 ¥1200（53 条使用记录）和游泳卡 ¥600（12/30 条使用记录），共 5 笔消费、65 条使用记录。

在模拟器内通过 Android 系统文件选择器导入后：总付款 ¥1862.00，健身年卡显示 53 次及 ¥22.64/次。随后通过系统文件创建器导出完整备份，拉取文件并逐个比较全部 86 行持久数据（含 12 分类、5 消费、2 多次详情、65 使用记录、2 设置），内容一致且 SHA-256 正确。

接着清空 App 数据，从刚才由 App 自己导出的文件恢复；再次显示 5 笔消费、65 条使用记录。强制停止并重新启动 App 后，¥1862.00、53 次和 ¥22.64/次仍然存在，验证了文件迁移与进程重启后的数据库持久性。

真机仍建议做一次最终验收：安装正式 APK → 新建普通消费和多次项目 → 记录/补录/修改使用 → 强制停止并重新启动 → 导出完整备份 → 在另一台手机恢复 → 比较数量与成本 → 检查系统文件选择器取消操作不改变数据。
